# =============================================================================
# SALESFORCE DATA CLOUD - DOCUMENT AI API TESTBED CONFIGURATION
# =============================================================================
# 
# IMPORTANT: This is an EXAMPLE file. You MUST create a copy named ".env" 
# and replace all placeholder values with your actual Salesforce credentials.
#
# Quick setup:
#   1. Run: cp .env.example .env
#   2. Open .env and replace the values below with your actual credentials
#   3. Restart the application
#
# =============================================================================

# OAuth Configuration
# -------------------
# Your Salesforce login URL (e.g., login.salesforce.com or your custom domain)
# For sandbox orgs, use: test.salesforce.com
# Example: LOGIN_URL=trailsignup-abc123.my.salesforce.com
LOGIN_URL=your-salesforce-login-url

# Your Salesforce Connected App Client ID (also called Consumer Key)
# Get this from Setup > Apps > App Manager > Your Connected App > View
CLIENT_ID=your-salesforce-connected-app-client-id

# Your Salesforce Connected App Client Secret (also called Consumer Secret)
# Get this from Setup > Apps > App Manager > Your Connected App > View
CLIENT_SECRET=your-salesforce-connected-app-client-secret

# Salesforce API Version (e.g., v64.0, v65.0, v66.0)
# Use v65.0 for Document AI extract-data; v66.0 can return 404 on some instances.
# v64.0+ for confidence scores; v66.0+ for page range selection (when available).
# Check versions: https://developer.salesforce.com/docs/atlas.en-us.api_rest.meta/api_rest/intro_versions.htm
API_VERSION=v65.0

# Optional: Document AI extract path (default uses .../actions/extract-data).
# If you get 404 with Document AI enabled, try: DOCUMENT_AI_EXTRACT_PATH=ssot/document-processing/extract-data
# DOCUMENT_AI_EXTRACT_PATH=ssot/document-processing/actions/extract-data

# Token Storage Configuration
# ---------------------------
# Local file where OAuth tokens will be stored (do not commit this file to git)
TOKEN_FILE=access-token.secret